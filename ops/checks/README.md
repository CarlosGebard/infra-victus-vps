# Checks

Validation scripts should live here.

Examples for later iterations:

- compose config validation
- open-port sanity checks
- backup verification
- host readiness checks

Current scripts:

- `validate-core.sh` renders the core Compose stack
- `validate-static.sh` runs all locally available static validation commands
