# Compose

This directory will contain runtime definitions for the single-node production stack.

Suggested next layout:

- `stacks/core/` main application stack
- `configs/nginx/` reverse proxy config
- `env/` non-secret environment templates
