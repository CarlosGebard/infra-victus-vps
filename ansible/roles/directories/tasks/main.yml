---
- name: Ensure infrastructure directories exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0750"
  loop:
    - "{{ srv_root }}"
    - "{{ srv_root }}/apps"
    - "{{ srv_root }}/apps/core"
    - "{{ srv_root }}/apps/core/nginx"
    - "{{ srv_root }}/apps/core/nginx/conf.d"
    - "{{ srv_root }}/apps/core/couchdb"
    - "{{ srv_root }}/apps/core/couchdb/local.d"
    - "{{ srv_root }}/apps/core/observability"
    - "{{ srv_root }}/apps/core/observability/loki"
    - "{{ srv_root }}/apps/core/observability/prometheus"
    - "{{ srv_root }}/apps/core/observability/grafana"
    - "{{ srv_root }}/apps/core/observability/grafana/provisioning"
    - "{{ srv_root }}/apps/core/observability/grafana/provisioning/datasources"
    - "{{ srv_root }}/data"
    - "{{ srv_root }}/data/couchdb"
    - "{{ srv_root }}/data/couchdb/data"
    - "{{ srv_root }}/data/seaweed"
    - "{{ srv_root }}/data/seaweed/data"
    - "{{ srv_root }}/data/infisical"
    - "{{ srv_root }}/data/infisical/postgres"
    - "{{ srv_root }}/data/infisical/redis"
    - "{{ srv_root }}/data/observability"
    - "{{ srv_root }}/data/observability/loki"
    - "{{ srv_root }}/data/observability/prometheus"
    - "{{ srv_root }}/data/observability/grafana"
    - "{{ srv_root }}/logs"
    - "{{ srv_root }}/logs/nginx"
    - "{{ srv_root }}/logs/observability"
    - "{{ srv_root }}/backups"
    - "{{ srv_root }}/secrets"
    - "{{ srv_root }}/secrets/bootstrap"
    - "{{ srv_root }}/secrets/runtime"
